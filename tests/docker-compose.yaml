services:
  netdata:
    build: ../netdata
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./options.json:/data/options.json
    pid: host
    network_mode: host
    privileged: true
    # cap_add:
    #   - SYS_PTRACE
    #   - SYS_ADMIN
    #   - SYS_RAWIO
